services:
  - type: web
    name: voice-note-backend
    runtime: docker
    plan: free
    region: frankfurt
    healthCheckPath: /health
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: USE_DIALECT_MODEL
        value: "true"
